name: Keep Render Alive

on:

  workflow_dispatch:

  schedule:
    - cron: "*/10 * * * *"

jobs:

  ping:

    runs-on: ubuntu-latest

    steps:

      - name: Ping Backend

        run: |

          curl https://canchabot.onrender.com/api/chat/health
